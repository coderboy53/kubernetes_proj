# Application functionalities
- get other app IP (to showcase loadbalancing by Services)
- save files (demonstrate ephemeral storage by default)
- save files to volumes (demo on volumes)
- store data to DB (interacting with applications on different namespaces)

# Troubleshooting scenarios:
## Pods
- CrashLoopBackOff: 
- ImagePullBackOff: configure wrong image in the deployment

# Things to create:
- StorageClass for PVCs
- PVCs for each deploy
- services for all deploys
- 