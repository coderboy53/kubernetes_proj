apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: greenapp-pvc
spec:
  resources:
    requests:
      storage: "500Mi"
  volumeMode: Filesystem
  volumeName: greenapp-pv
  storageClassName: webapp-sc
  accessModes:
    - ReadWriteOnce
